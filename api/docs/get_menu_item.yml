Get Menu Item
---
tags:
  - Menu
parameters:
  - name: Authorization
    in: header
    description: Authorization header required for user authentication
    required: true
    type: string
responses:
  200:
    description: You have got all menu items
security:
  - JWT:
    description: Pass in jwt token i.e Bearer <jwt>
    type: apiKey
    scheme: bearer
    name: Authorization
    in: header
    template: "Bearer {apiKey}"
    bearerFormat: JWT